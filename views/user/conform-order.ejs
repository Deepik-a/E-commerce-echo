<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        :root {
            --green-lightest: #E6F3E7;
            --green-light: #A5D6A7;
            --green-medium: #66BB6A;
            --green-dark: #2E7D32;
            --green-darkest: #1B5E20;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, var(--green-lightest), var(--green-light));
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            perspective: 1000px;
            background-image: url('../../public/assets/street-market-is-full-fruits-vegetables-sale.jpg');
            background-position: center;
    background-size: cover;
    background-repeat: no-repeat;
    box-sizing: border-box;
  
        }

        .success-container {
            background: linear-gradient(to bottom right, var(--green-medium), var(--green-dark));
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.15);
            padding: 2.5rem;
            max-width: 500px;
            width: 90%;
            text-align: center;
            position: relative;
            overflow: hidden;
            color: white;
            transform: translateY(20px);
            opacity: 0;
            animation: float-in 1s ease-out forwards;
        }

        @keyframes float-in {
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }

        .sapling-animation {
            position: absolute;
            top: -40px;
            right: -40px;
            width: 180px;
            height: 180px;
            opacity: 0.7;
        }

        .sapling {
            stroke: var(--green-darkest);
            stroke-width: 3;
            fill: none;
            animation: grow-sapling 2s ease-out forwards;
        }

        @keyframes grow-sapling {
            0% {
                stroke-dashoffset: 250;
                opacity: 0;
            }
            100% {
                stroke-dashoffset: 0;
                opacity: 1;
            }
        }

        .checkmark {
            margin-bottom: 1rem;
        }

        .checkmark__circle {
            stroke: var(--green-lightest);
            stroke-width: 4;
            stroke-dasharray: 166;
            stroke-dashoffset: 166;
            stroke-linecap: round;
            animation: draw-circle 1s ease-out forwards;
        }

        .checkmark__check {
            stroke: var(--green-lightest);
            stroke-width: 4;
            stroke-dasharray: 48;
            stroke-dashoffset: 48;
            stroke-linecap: round;
            animation: draw-check 1s 0.5s ease-out forwards;
        }

        @keyframes draw-circle {
            to { stroke-dashoffset: 0; }
        }

        @keyframes draw-check {
            to { stroke-dashoffset: 0; }
        }

        .btn-custom {
            background: var(--green-darkest);
            color: var(--green-lightest);
            border: 2px solid var(--green-lightest);
            transition: all 0.3s ease;
        }

        .btn-custom:hover {
            background: var(--green-light);
            color: var(--green-darkest);
            transform: scale(1.05);
        }

        @media (max-width: 600px) {
            .success-container {
                width: 95%;
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="success-container">
        <svg class="sapling-animation" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
            <path class="sapling" d="M50 90 
                C30 70, 20 50, 50 30 
                C80 50, 70 70, 50 90 
                M50 30 L50 10 
                M40 20 L60 20" 
                stroke-dasharray="250" 
                stroke-dashoffset="250"/>
        </svg>

        <svg class="checkmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
            <circle class="checkmark__circle" cx="26" cy="26" r="22" fill="none" />
            <path class="checkmark__check" fill="none" d="M14.1 27.2l7.1 7.2 16.7-16.8" />
        </svg>

        <h1>Thank you.</h1>
        <h2 class="mb-4">Order Successfully Placed!</h2>
        
        <p class="mb-4">
            Your purchase supports our mission. We're planting a sapling with every order and delivering one to you!
        </p>
        
        <p class="mb-4">
            Your order will be processed immediately and delivered in 2-5 business days.
        </p>

        <div class="d-flex justify-content-center gap-3">
            <a href="/orders" class="btn btn-custom">My Orders</a>
            <a href="/all-products" class="btn btn-custom">Continue Shopping</a>
        </div>
    </div>
</body>
</html>