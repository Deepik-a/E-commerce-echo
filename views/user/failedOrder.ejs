<!DOCTYPE html>
<html>
<head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        @keyframes scaleIn {
            to { transform: scale(1); }
        }
        @keyframes drawCircle {
            to { stroke-dashoffset: 0; }
        }
        @keyframes drawCross {
            to { stroke-dashoffset: 0; }
        }
        @keyframes fadeIn {
            to { opacity: 1; }
        }
        .animate-scale-in {
            transform: scale(0);
            animation: scaleIn 0.5s forwards ease-out;
        }
        .animate-draw-circle {
            stroke-dasharray: 166;
            stroke-dashoffset: 166;
            animation: drawCircle 1s ease-out forwards;
        }
        .animate-draw-cross {
            stroke-dasharray: 48;
            stroke-dashoffset: 48;
            animation: drawCross 1s 0.5s ease-out forwards;
        }
        .animate-fade-in {
            opacity: 0;
            animation: fadeIn 1s forwards;
        }
        .animate-fade-in-delay {
            opacity: 0;
            animation: fadeIn 1s 1s forwards;
        }
    </style>
</head>
<body class="d-flex justify-content-center align-items-center min-vh-100 bg-dark text-white m-0">
    <div class="text-center bg-secondary p-5 rounded shadow-lg animate-scale-in">
        <div class="mb-4">
            <svg class="mx-auto" width="96" height="96" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
                <circle class="animate-draw-circle" cx="26" cy="26" r="22" 
                        style="stroke: #f44336; stroke-width: 4; fill: none;"></circle>
                <path class="animate-draw-cross" d="M16 16 36 36 M36 16 16 36" 
                      style="stroke: #f44336; stroke-width: 4; fill: none;"></path>
            </svg>
        </div>
        
        <h1 class="text-white fw-bold mb-2">Sorry.</h1>
        <h1 class="text-danger fw-bold mb-2 animate-fade-in">Order Failed!</h1>
        <p class="text-white mb-4 animate-fade-in-delay">
            Unfortunately, your order could not be processed. Please try again later.
        </p>
        <p class="text-secondary mb-5">
            If you continue to experience issues, please contact our support team for assistance.
        </p>
        
        <div class="d-flex justify-content-center gap-3">
            <a href="/orders" class="btn btn-danger px-4 py-2">
                My Orders
            </a>
            <a href="/home" class="btn btn-danger px-4 py-2">
                Continue Shopping
            </a>
        </div>
    </div>
</body>
</html>
